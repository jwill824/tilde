---
phase: 01-tool-metadata-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/tools/metadata.ts
  - src/tools/registry.ts
  - tests/unit/tool-metadata.test.ts
  - tests/unit/browser-step.test.tsx
  - tests/integration/wizard-flow.test.tsx
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
user_setup: []
must_haves:
  truths:
    - "Registry aggregate validation catches missing required fields, duplicate ids, malformed install ids, malformed paths, and invalid platforms before downstream use per D-10, D-11, and D-12."
    - "Registry tests prove the aggregate contains both plugin-backed browser metadata and non-plugin note-taking metadata per D-03."
    - "Lookup helpers remain broad enough for later inventory, dotfile, and provenance phases per D-13 and D-14."
    - "Browser wizard behavior has unit and integration regression coverage after migration per D-07 and D-09."
    - "The phase can pass the validation contract commands from `01-VALIDATION.md`."
  artifacts:
    - path: "tests/unit/tool-metadata.test.ts"
      provides: "Strict negative validation and broad lookup coverage"
    - path: "tests/unit/browser-step.test.tsx"
      provides: "BrowserStep behavior regression coverage"
    - path: "tests/integration/wizard-flow.test.tsx"
      provides: "Wizard-level browser prompt regression coverage"
    - path: "src/tools/metadata.ts"
      provides: "Any validation refinements required by negative tests"
    - path: "src/tools/registry.ts"
      provides: "Any lookup helper refinements required by broad query tests"
    - path: "src/tools/note-taking-metadata.ts"
      provides: "Non-plugin note-taking metadata seed verified by registry tests"
  key_links:
    - from: "tests/unit/tool-metadata.test.ts"
      to: "src/tools/metadata.ts"
      via: "ToolMetadataSchema.safeParse"
      pattern: "ToolMetadataSchema"
    - from: "tests/unit/tool-metadata.test.ts"
      to: "src/tools/registry.ts"
      via: "lookup helper imports"
      pattern: "getToolsBy"
    - from: "tests/integration/wizard-flow.test.tsx"
      to: "src/steps/browser.tsx"
      via: "Ink render"
      pattern: "BrowserStep"
    - from: "tests/unit/tool-metadata.test.ts"
      to: "src/tools/note-taking-metadata.ts"
      via: "non-plugin metadata import"
      pattern: "noteTakingToolMetadata"
---

## Phase Goal

**As a** tilde developer, **I want to** maintain wizard tool metadata in one shared registry, **so that** current steps can consume canonical tool facts without user-visible behavior changes.

<objective>
Harden metadata validation and regression tests so malformed registry data or browser behavior drift fails fast.

Purpose: This closes META-01 through META-05 by enforcing strict validation, proving broad lookup behavior, and sampling the migrated browser UI at both unit and integration levels. It also maps directly to validation threats T-01-01, T-01-02, and T-01-03 from `01-VALIDATION.md`.
Output: Strengthened metadata tests, any schema/registry refinements needed to satisfy them, and browser wizard regression coverage.
</objective>

<execution_context>
@/Users/jeff.williams/Developer/personal/tilde/.codex/gsd-core/workflows/execute-plan.md
@/Users/jeff.williams/Developer/personal/tilde/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-tool-metadata-registry/01-CONTEXT.md
@.planning/phases/01-tool-metadata-registry/01-RESEARCH.md
@.planning/phases/01-tool-metadata-registry/01-VALIDATION.md
@.planning/phases/01-tool-metadata-registry/01-PATTERNS.md
@.planning/phases/01-tool-metadata-registry/01-01-SUMMARY.md
@.planning/phases/01-tool-metadata-registry/01-02-SUMMARY.md
@src/tools/metadata.ts
@src/tools/registry.ts
@src/tools/note-taking-metadata.ts
@src/plugins/first-party/browser/metadata.ts
@src/steps/browser.tsx
@src/steps/tools.tsx
@tests/unit/tool-metadata.test.ts
@tests/unit/browser-step.test.tsx
@tests/integration/wizard-flow.test.tsx
</context>

## Artifacts this phase produces

- `src/tools/metadata.ts`: `PlatformSchema`, `ToolCategorySchema`, `ToolInstallSchema`, `ToolExternalIdsSchema`, `ToolMetadataSchema`, `ToolMetadataArraySchema`, `ToolPlatform`, `ToolCategory`, `ToolSource`, `ToolMetadata`, `validateToolMetadata`.
- `src/tools/registry.ts`: `allToolMetadata`, `getToolMetadata`, `getToolsByCategory`, `getToolsByPlatform`, `getToolsByHomebrewFormula`, `getToolsByHomebrewCask`, `getToolsByHomebrewId`, `getToolsByConfigPath`, `getToolsByDotfilePath`, `getToolsByVariant`, `getToolsBySource`, `searchTools`.
- `src/plugins/first-party/browser/metadata.ts`: `browserToolMetadata`.
- `src/tools/note-taking-metadata.ts`: `noteTakingToolMetadata` for Obsidian, Notion, and Bear as the D-03 non-plugin catalog.
- `tests/unit/tool-metadata.test.ts`: schema validation, registry loading, uniqueness, lookup, and malformed fixture coverage.
- `tests/unit/browser-step.test.tsx`: registry-backed BrowserStep regression tests.
- Updated `src/steps/browser.tsx`: BrowserStep consumes registry metadata.
- Updated `src/plugins/first-party/browser/index.ts`: `BROWSER_PLUGINS` fields align with browser metadata.
- Updated `tests/unit/browser-plugins.test.ts` and `tests/integration/wizard-flow.test.tsx`: plugin alignment and wizard regression coverage, completed by this phase.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden metadata validation and lookup coverage</name>
  <files>src/tools/metadata.ts, src/tools/registry.ts, tests/unit/tool-metadata.test.ts</files>
  <read_first>
    - src/tools/metadata.ts
    - src/tools/registry.ts
    - tests/unit/tool-metadata.test.ts
    - src/plugins/first-party/browser/metadata.ts
    - src/tools/note-taking-metadata.ts
    - .planning/phases/01-tool-metadata-registry/01-VALIDATION.md
    - .planning/phases/01-tool-metadata-registry/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: duplicate metadata ids fail aggregate validation with a descriptive error.
    - Test 2: invalid supported platforms, empty labels, control-character labels, blank Homebrew formula/cask ids, and blank path entries fail validation.
    - Test 3: the aggregate contains browser metadata and note-taking metadata, including Bear without a Homebrew cask, proving D-03 is covered by real data rather than only schema capability.
    - Test 4: `getToolsByConfigPath` and `getToolsByDotfilePath` match exact paths and child paths when fixture metadata provides those fields.
    - Test 5: `getToolsByVariant`, `getToolsBySource`, and `searchTools` behave deterministically and return empty arrays for blank or unmatched queries.
  </behavior>
  <action>Extend `tests/unit/tool-metadata.test.ts` with malformed fixtures and focused lookup fixtures for META-01, META-02, META-04, and META-05. Include assertions that import `noteTakingToolMetadata`, prove `getToolsByCategory('note-taking')` returns Obsidian, Notion, and Bear, and prove Bear is valid without a Homebrew cask per D-03. If the new tests expose missing validation, update `src/tools/metadata.ts` using Zod refinements rather than ad hoc validators. If lookup tests expose missing behavior, update `src/tools/registry.ts` while keeping helpers pure and side-effect-free. Preserve optional Phase 1 fields as optional: `configPaths`, `dotfilePaths`, and richer provenance hints may be absent or empty, but must validate when provided per D-12.</action>
  <verify>
    <automated>npm run test -- tests/unit/tool-metadata.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `npm run test -- tests/unit/tool-metadata.test.ts` passes.
    - Behavior assertions: duplicate ids and malformed fields fail validation before downstream use.
    - Behavior assertions: D-03 is covered by real note-taking metadata, not only a generic schema fixture.
    - Source assertions: registry helpers still do not import filesystem APIs, command runners, Ink, or `PluginRegistry`.
    - Requirement assertions: test names or descriptions cover META-01, META-02, META-04, and META-05.
  </acceptance_criteria>
  <done>Metadata validation and lookup tests protect the aggregate registry against malformed data and future helper drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen browser wizard regression coverage and phase gates</name>
  <files>tests/unit/browser-step.test.tsx, tests/integration/wizard-flow.test.tsx</files>
  <read_first>
    - tests/unit/browser-step.test.tsx
    - tests/integration/wizard-flow.test.tsx
    - src/steps/browser.tsx
    - src/tools/registry.ts
    - src/plugins/first-party/browser/metadata.ts
    - .planning/phases/01-tool-metadata-registry/01-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `tests/unit/browser-step.test.tsx` covers registry-backed labels, selection toggling, install calls, and default-browser completion payloads.
    - Test 2: `tests/integration/wizard-flow.test.tsx` browser test asserts actual browser labels such as `Safari` and `Google Chrome`, not only that a frame string exists.
    - Test 3: Targeted validation command from `01-VALIDATION.md` passes: `npm run test -- tests/unit/tool-metadata.test.ts tests/unit/browser-step.test.tsx`.
  </behavior>
  <action>Refine `tests/unit/browser-step.test.tsx` if Task 1 changes registry behavior. Update the existing browser test in `tests/integration/wizard-flow.test.tsx` so it waits for detection, asserts visible browser labels from shared metadata, and verifies the prompt still shows terminal instructions. Keep mocks hoisted before dynamic imports and avoid real external commands. Run the full phase gate commands after targeted tests pass.</action>
  <verify>
    <automated>npm run test -- tests/unit/tool-metadata.test.ts tests/unit/browser-step.test.tsx</automated>
    <automated>npm run test:integration -- tests/integration/wizard-flow.test.tsx</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Test-command: `npm run test -- tests/unit/tool-metadata.test.ts tests/unit/browser-step.test.tsx` passes, matching `01-VALIDATION.md`.
    - Test-command: `npm run test:integration -- tests/integration/wizard-flow.test.tsx` passes.
    - CLI output: no test output shows real `brew`, `defaultbrowser`, `gh`, `op`, or `vfox` execution.
    - Source assertions: integration browser test contains concrete label expectations for the migrated browser step.
  </acceptance_criteria>
  <done>The validation contract is green and browser UI regression coverage checks actual post-migration behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Malformed fixtures -> schema refinements | Tests intentionally cross invalid metadata into validation logic. |
| Migrated BrowserStep -> wizard integration test | Registry data is rendered in the broader wizard flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | `allToolMetadata` aggregate validation | mitigate | Add duplicate id, malformed id/path/platform/install-id tests and update schema/registry validation so aggregate failures happen before downstream use. |
| T-01-02 | Spoofing | `BrowserStep` terminal output | mitigate | Integration tests assert concrete browser labels and prompt instructions after registry migration. |
| T-01-01 | Tampering | lookup helper inputs | mitigate | Tests cover blank queries, unmatched values, exact/child path matching, and deterministic source/variant/search behavior. |
| T-01-SC | Tampering | package installs | accept | This plan adds no npm/pip/cargo install task and keeps all external command behavior under mocks. |
</threat_model>

<verification>
- Run `npm run test -- tests/unit/tool-metadata.test.ts` after metadata validation hardening.
- Run `npm run test -- tests/unit/tool-metadata.test.ts tests/unit/browser-step.test.tsx` after browser test refinements.
- Run `npm run test:integration -- tests/integration/wizard-flow.test.tsx`, `npm run lint`, `npm run build`, and `npm test` before marking the phase ready for verification.
</verification>

<success_criteria>
- META-01 through META-05 all have automated coverage in the phase validation commands.
- T-01-01, T-01-02, and T-01-03 from `01-VALIDATION.md` map to concrete tests.
- The final phase gate can run without watch-mode flags and without real external command execution.
</success_criteria>

<output>
Create `.planning/phases/01-tool-metadata-registry/01-03-SUMMARY.md` when done.
</output>
