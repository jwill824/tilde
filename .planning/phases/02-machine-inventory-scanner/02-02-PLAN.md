---
phase: 02-machine-inventory-scanner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/first-party/homebrew/metadata.ts
  - src/plugins/first-party/vfox/metadata.ts
  - src/plugins/first-party/vscode/metadata.ts
  - src/plugins/first-party/neovim/metadata.ts
  - src/plugins/first-party/jetbrains/metadata.ts
  - src/plugins/first-party/cursor/metadata.ts
  - src/plugins/first-party/zed/metadata.ts
  - src/tools/registry.ts
  - tests/unit/tool-metadata.test.ts
autonomous: true
requirements:
  - INV-01
user_setup: []
must_haves:
  truths:
    - "D-05: Registry metadata covers plugin-backed package-manager, version-manager, and editor rows needed by INV-01."
    - "D-06: Inventory can consume these rows through the existing pure registry without broadening PluginRegistry."
    - "D-02: Metadata rows describe install/config evidence only and do not introduce final provenance labels."
  artifacts:
    - path: "src/plugins/first-party/homebrew/metadata.ts"
      provides: "homebrewToolMetadata"
    - path: "src/plugins/first-party/vfox/metadata.ts"
      provides: "vfoxToolMetadata"
    - path: "src/plugins/first-party/vscode/metadata.ts"
      provides: "vscodeToolMetadata"
    - path: "src/plugins/first-party/neovim/metadata.ts"
      provides: "neovimToolMetadata"
    - path: "src/plugins/first-party/jetbrains/metadata.ts"
      provides: "jetbrainsToolMetadata"
    - path: "src/plugins/first-party/cursor/metadata.ts"
      provides: "cursorToolMetadata"
    - path: "src/plugins/first-party/zed/metadata.ts"
      provides: "zedToolMetadata"
    - path: "src/tools/registry.ts"
      provides: "aggregate metadata registry including Phase 2 plugin-backed rows"
  key_links:
    - from: "src/tools/registry.ts"
      to: "src/plugins/first-party/*/metadata.ts"
      via: "ToolMetadata[] imports aggregated through validateToolMetadata"
      pattern: "validateToolMetadata"
    - from: "tests/unit/tool-metadata.test.ts"
      to: "src/tools/registry.ts"
      via: "allToolMetadata and category/Homebrew lookup assertions"
      pattern: "allToolMetadata|getToolsByCategory|getToolsByHomebrew"
---

## Phase Goal

**As a** tilde user, **I want to** see known installed-tool facts before choosing setup options, **so that** I can trust what tilde will manage before it changes anything.

<objective>
Seed the shared metadata registry with the plugin-backed tool rows Phase 2 inventory needs: Homebrew, vfox, VS Code, Neovim, JetBrains Toolbox, Cursor, and Zed. This plan implements D-05 and supports INV-01 without moving command execution into the registry.

Purpose: let the inventory scanner classify package managers, version managers, and editors through the Phase 1 registry pattern.
Output: family-owned metadata arrays, registry aggregation updates, and metadata regression tests.
</objective>

<execution_context>
@/Users/jeff.williams/Developer/personal/tilde/.codex/gsd-core/workflows/execute-plan.md
@/Users/jeff.williams/Developer/personal/tilde/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-machine-inventory-scanner/02-CONTEXT.md
@.planning/phases/02-machine-inventory-scanner/02-RESEARCH.md
@.planning/phases/02-machine-inventory-scanner/02-PATTERNS.md
@.planning/phases/01-tool-metadata-registry/01-01-SUMMARY.md
@.planning/phases/01-tool-metadata-registry/01-02-SUMMARY.md
@.planning/phases/01-tool-metadata-registry/01-03-SUMMARY.md
</context>

## Artifacts this phase produces

- New metadata exports: `homebrewToolMetadata`, `vfoxToolMetadata`, `vscodeToolMetadata`, `neovimToolMetadata`, `jetbrainsToolMetadata`, `cursorToolMetadata`, `zedToolMetadata`.
- Registry aggregation in `src/tools/registry.ts` that validates these rows with existing `validateToolMetadata`.
- Unit coverage in `tests/unit/tool-metadata.test.ts` for aggregate ids, categories, platform support, and Homebrew formula/cask lookup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing metadata seed coverage for inventory categories</name>
  <files>tests/unit/tool-metadata.test.ts</files>
  <read_first>
@.planning/phases/02-machine-inventory-scanner/02-CONTEXT.md
@.planning/phases/02-machine-inventory-scanner/02-PATTERNS.md
@src/tools/metadata.ts
@src/tools/registry.ts
@src/plugins/first-party/browser/metadata.ts
@src/plugins/first-party/homebrew/index.ts
@src/plugins/first-party/vfox/index.ts
@src/plugins/first-party/vscode/index.ts
@src/plugins/first-party/neovim/index.ts
@src/plugins/first-party/jetbrains/index.ts
@src/plugins/first-party/cursor/index.ts
@src/plugins/first-party/zed/index.ts
@tests/unit/tool-metadata.test.ts
  </read_first>
  <behavior>
- `allToolMetadata` includes Phase 2 rows for package manager, version manager, and editor coverage per INV-01 and D-05.
- `getToolsByCategory('package-manager')` includes Homebrew and `getToolsByCategory('version-manager')` includes vfox.
- `getToolsByCategory('editor')` includes VS Code, Neovim, JetBrains Toolbox, Cursor, and Zed.
- Homebrew formula/cask lookup returns the expected metadata rows without calling external commands.
  </behavior>
  <action>Extend `tests/unit/tool-metadata.test.ts` with failing assertions for the new metadata ids and category lookups. Assert specific labels and Homebrew identifiers derived from the existing first-party plugin modules. Keep tests pure: do not import plugin behavior modules for command execution and do not run Homebrew. Cite D-05 and INV-01 in test descriptions or assertion names.</action>
  <verify>
    <automated>npm run test -- tests/unit/tool-metadata.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: `tests/unit/tool-metadata.test.ts` expects Homebrew under `package-manager`, vfox under `version-manager`, and at least five editor ids under `editor`.
- Source assertion: tests verify at least one Homebrew formula lookup and one Homebrew cask lookup from the new rows.
- Behavior assertion: the targeted test command is RED before metadata files and registry imports exist.
  </acceptance_criteria>
  <done>Failing registry tests specify the plugin-backed metadata rows inventory needs for INV-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed plugin-backed metadata arrays and aggregate them</name>
  <files>src/plugins/first-party/homebrew/metadata.ts, src/plugins/first-party/vfox/metadata.ts, src/plugins/first-party/vscode/metadata.ts, src/plugins/first-party/neovim/metadata.ts, src/plugins/first-party/jetbrains/metadata.ts, src/plugins/first-party/cursor/metadata.ts, src/plugins/first-party/zed/metadata.ts, src/tools/registry.ts, tests/unit/tool-metadata.test.ts</files>
  <read_first>
@src/tools/metadata.ts
@src/tools/registry.ts
@src/plugins/first-party/browser/metadata.ts
@src/plugins/first-party/homebrew/index.ts
@src/plugins/first-party/vfox/index.ts
@src/plugins/first-party/vscode/index.ts
@src/plugins/first-party/neovim/index.ts
@src/plugins/first-party/jetbrains/index.ts
@src/plugins/first-party/cursor/index.ts
@src/plugins/first-party/zed/index.ts
@tests/unit/tool-metadata.test.ts
  </read_first>
  <behavior>
- Each new family metadata file exports a typed `ToolMetadata[]` array using existing Phase 1 metadata style.
- Registry aggregation imports all new arrays and still validates through `validateToolMetadata`.
- Metadata rows include stable ids, labels, categories, supported platform `darwin`, source `first-party` where appropriate, and install identifiers used by the existing plugin modules.
- No scanner logic, plugin behavior, or UI code is modified in this plan.
  </behavior>
  <action>Create colocated metadata files for Homebrew, vfox, VS Code, Neovim, JetBrains Toolbox, Cursor, and Zed. Use category values already allowed by `ToolCategorySchema`: `package-manager`, `version-manager`, and `editor`. Derive ids, labels, app paths, and Homebrew formula/cask identifiers from the corresponding `index.ts` plugin modules; expected stable install identifiers include `vfox`, `visual-studio-code`, `neovim`, `jetbrains-toolbox`, `cursor`, and `zed` where the plugin module confirms them. Update `src/tools/registry.ts` to import and spread these arrays beside browser and note-taking metadata. Keep registry helpers pure and side-effect free per Phase 1 decisions and D-06; do not broaden `PluginRegistry`.</action>
  <verify>
    <automated>npm run test -- tests/unit/tool-metadata.test.ts -x</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- Source assertion: each new metadata file exports a named `*ToolMetadata` array and imports `type ToolMetadata` with a `.js` extension where applicable.
- Source assertion: `src/tools/registry.ts` spreads all seven new arrays into `validateToolMetadata`.
- Behavior assertion: `npm run test -- tests/unit/tool-metadata.test.ts -x` passes with the new category and lookup assertions.
- Behavior assertion: `npm run build` passes with NodeNext import extensions.
  </acceptance_criteria>
  <done>Plugin-backed metadata rows are validated through the shared registry and ready for inventory consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static metadata -> registry consumers | Incorrect metadata can mislead inventory facts and user-facing summaries. |
| Plugin behavior -> metadata extraction | Plugin modules contain side effects; metadata must remain static data and not execute plugin behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | `src/tools/registry.ts` metadata aggregation | mitigate | Keep all rows validated through `validateToolMetadata`; extend unit tests for expected ids and lookup paths. |
| T-02-06 | Spoofing | New metadata labels/install ids | mitigate | Derive labels and Homebrew identifiers from existing first-party plugin modules and test exact expected rows. |
| T-02-07 | Information Disclosure | Metadata files | accept | Static metadata contains install/config paths only; no raw secrets or user files are read in this plan. |
| T-02-SC | Tampering | npm installs | accept | No package-manager install tasks exist in this plan; package legitimacy audit in research lists no new packages. |
</threat_model>

<verification>
- Targeted unit: `npm run test -- tests/unit/tool-metadata.test.ts -x`
- Build: `npm run build`
- Phase gate contribution: `npm run lint`
</verification>

<success_criteria>
- Registry metadata covers Homebrew, vfox, VS Code, Neovim, JetBrains Toolbox, Cursor, and Zed.
- Existing registry validation still rejects malformed metadata before consumers use it.
- Inventory can later read package-manager, version-manager, and editor rows from `allToolMetadata`.
- No command execution or PluginRegistry expansion is introduced.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| REQ | INV-01 | Detect package managers, version managers, shells, editors, and core tools | 02-01, 02-02 | COVERED | This plan provides plugin-backed metadata rows; 02-01 provides scanner-owned shell/core facts. |
| CONTEXT | D-02 | No final provenance labels | 02-02 | COVERED | Metadata contains install/config evidence only. |
| CONTEXT | D-05 | Registry categories plus scanner-owned shell/core | 02-02 | COVERED | Package manager, version manager, and editor rows are registry-owned. |
| CONTEXT | D-06 | Dedicated inventory consumes registry | 02-02 | COVERED | Registry remains pure; inventory reads it in 02-01 and later plans. |
| RESEARCH | R-01 | Keep registry pure and command execution in helpers | 02-02 | COVERED | Only static metadata and registry aggregation change. |
| RESEARCH | R-05 | Seed targeted plugin-backed metadata rows | 02-02 | COVERED | Research open question resolved with targeted rows, not broad catalog work. |
</source_audit>

<output>
Create `.planning/phases/02-machine-inventory-scanner/02-02-SUMMARY.md` when done.
</output>
