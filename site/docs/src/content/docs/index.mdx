---
title: Welcome to tilde
description: tilde is a public proof of concept for macOS developer environment inventory, provenance, and bootstrap workflows.
template: splash
hero:
  tagline: Public proof of concept for understanding and managing macOS developer environments.
  actions:
    - text: Getting Started
      link: /tilde/docs/getting-started/
      icon: right-arrow
    - text: Project Status
      link: /tilde/docs/installation/
      icon: document
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## tilde provenance report

tilde is currently a public proof of concept. It has been live while the inventory
and provenance model is being developed, but it is not published to npm yet.

| Signal | Current value |
|--------|---------------|
| Stage | Public PoC |
| Origin | GitHub repository |
| Distribution | Source only |
| npm package | Unpublished |
| Hosted installer | Parked until release |
| Docs | Live |

Package-manager installation with `npm install -g @jwill824/tilde`, `npx`, or the
hosted curl installer is not available until the first package release.

## Why tilde?

<CardGrid stagger>
  <Card title="Configuration-First" icon="setting">
    Define your entire dev environment in a single `tilde.config.json`. Every tool,
    language, and preference is version-controlled and reproducible.
  </Card>
  <Card title="Idempotent" icon="approve-check">
    Run tilde as many times as you want. It checks what's already installed and only
    applies what's missing — safe to re-run after any change.
  </Card>
  <Card title="Extensible" icon="puzzle">
    Install only what you need. tilde is built around a plugin-style tool list —
    add languages, tools, and accounts as your setup evolves.
  </Card>
  <Card title="Provenance-aware" icon="magnifier">
    tilde is being designed to explain whether tools are managed by tilde, manually
    installed, bundled with macOS, or present as dependencies before changing a machine.
  </Card>
</CardGrid>
